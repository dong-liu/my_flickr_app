define([], function() {

	var CONFIGURATION = {
		
		// Default search fitler to be used for Flicker Public Feed call
		filter: {
			tags: 'dog'
		},

		// Default date format used within the application
		dateFormat: 'MMM Do YYYY, h:mm:ss A'

	};

	return CONFIGURATION;
});